export enum DeckType {
  FIBONNACI = "FIBONACCI",
  TSHIRT_SIZE = "TSHIRT",
  LINEAR = "LINEAR",
}

export enum RoomVisibility {
  PRIVATE = "PRIVATE",
  PUBLIC = "PUBLIC",
}

export enum VoteMode {
  ASYNC = "ASYNC",
  LIVE_REVEAL = "LIVE_REVEAL",
}

